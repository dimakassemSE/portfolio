
import logo from "../asserts/icon/logo.svg";

export const LogoIcon = () => {
    return (
        <img
            src={logo}
            className="d-inline-block align-top"
            alt="logo"
        />
    )
}